--use CarRental
--ALTER TABLE Customers
--     	FOREIGN KEY (PersonID) REFERENCES People(PersonID)


--		ALTER TABLE Customers
--     	drop  Name,ContactInformation; 

--		CREATE TABLE Customerrs (
--    CustomerID INT PRIMARY KEY,
--	  PersonID INT NOT NULL,
--	 FOREIGN KEY (PersonID) REFERENCES People(PersonID),
--    DriverLicenseNumber NVARCHAR(20)
--)

--alter table RentalBooking
--add
--  CustomerID INT FOREIGN KEY REFERENCES Customerrs(CustomerID)

------------------
		select * from people
		SELECT * FROM users
select * from Customerrs
select * from RentalBooking
select * from Vehicles










